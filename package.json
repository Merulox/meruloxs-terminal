{
  "name": "home-merulox-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist --project-name merulox --branch main --commit-dirty=true",
    "astro": "astro",
    "update:goodreads": "node scripts/update-goodreads.mjs"
  },
  "dependencies": {
    "astro": "^6.4.4"
  }
}
