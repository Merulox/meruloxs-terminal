# merulox.com

Static Astro site for Merulox: a dark, minimal personal builder and resume website.

## Commands

```sh
npm install
npm run dev
npm run build
```

## Deploy

Cloudflare Pages settings:

- Build command: `npm run build`
- Output directory: `dist`
- Node version: `22.12.0` or newer
